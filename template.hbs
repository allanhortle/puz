<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<style>
* {
    margin: 0;
    font-size: inherit;
}

body {
    font-family: -apple-system, BlinkMacSystemFont;
    font-size: 14px;
    line-height: 1.25;
    width: 210mm;
    height: 297mm;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
}

.wrapper {
    display: flex;
}

.across {
    flex-basis: 30%;
    flex-shrink: 0;
    padding-right: 1rem;
}

.crossword {
    width: 100%;
    aspect-ratio: 1/1;
    border: 1px solid;
    display: grid; 
    grid-template-columns: repeat({{width}}, 1fr);
    margin-bottom: 1rem;
}

.black {
    background-color: black;
    outline: 1px solid;
}

.white {
    border: 1px solid;
    line-height: 1;
    font-size: 12px;
}

.down {
    columns: 3;
}

h1 {
    font-size: 1.5rem;
    line-height: 1;
    margin-bottom: .25rem;
}

h2 {
    font-weight: normal;
    margin-bottom: 1rem;
}
</style>
<body>
<div class="wrapper">
    <div class="across">
        <h1>{{title}}</h1> 
        <h2>{{author}}</h2>
        <strong>Across</strong>
        {{#each across}}<div><strong>{{@index}}</strong> {{{this}}}</div>{{/each}}
    </div>
    <div>
        <div class="crossword">
            {{#each board}}
                {{#if black}}
                    <div class="black"></div>
                {{else}}
                    <div class="white">{{number}}</div>
                {{/if}}
            {{/each}}

        </div>
        <div class="down">
            <strong>Down</strong>
            {{#each down}}<div><strong>{{@index}}</strong> {{{this}}}</div>{{/each}}
        </div>
    </div>
</div>
</body>
</html>
